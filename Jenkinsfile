 pipeline {
  agent any //{
    //docker { image 'node:16-alpine' }
  //}
  stages {
    stage('Test') {
      steps {
        echo $PATH
        echo $Path 
        sh 'node --version'
      }
    }
  }
}
